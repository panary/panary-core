# audit-events-domain

Append-only Audit-Trail-Schemas, Enums und Resource-Whitelist für Tenant-Level-Audits.

## Building

Run `nx build audit-events-domain` to build the library.

## Running unit tests

Run `nx test audit-events-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
