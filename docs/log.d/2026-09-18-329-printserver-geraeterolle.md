* **Update**: [Edge-authorize — Hybrid-RBAC](../security/edge-authorize-hybrid-rbac.md) um den Nachtrag „der Print-Pfad fragte die Matrix mit der falschen Rolle" erweitert. `printServerAuth` baute den virtuellen Geräte-User aus `keyRecord.deviceRole` — ein Feld, das es auf einem `apikeys`-Record nie gab (`apikeySchema` kennt nur `role` und ist `additionalProperties: false`; `deviceRole` entsteht erst auf der Socket-Connection in `channels.ts`). Der Ausdruck war damit ausnahmslos `undefined`, und der `|| DEVICE_POS`-Fallback machte aus **jedem** Schlüssel ein POS-Gerät. Die Rechteprüfung lief seit dem Umbau vom 2026-08-04 korrekt, sie beantwortete nur immer dieselbe Frage.
* **Update**: `DEVICE_TABLET` und `DEVICE_KIOSK` haben in `roles.matrix.ts` jetzt `PRINT_SERVER: CREATE`. Ohne diesen Zusatz hätte ausgerechnet die Korrektur ihnen den Bondruck genommen — beide druckten bisher nur über den Fallback, und die Rollen entstehen real: `apikeys.schema.ts` leitet `role` aus `device.type` ab, der Typ kommt beim Pairing aus dem Client-Request. `CREATE` bleibt es bewusst: `/start`, `/stop` und `/restart` verlangen `MANAGE` und gehören weiter dem Admin-Panel über den JWT-Zweig.
* **Update**: Kein Fallback mehr auf `DEVICE_POS`. Ein Record ohne `role` erbte vorher lautlos Druckrechte; jetzt bleibt die Rolle `undefined`, `hasEffectivePermission` liefert `false` und der Aufruf endet mit 403. Das Wide-Event `print-server.forbidden` trägt die Rolle als `user.role ?? null`, weil ein fehlendes Feld im JSON sonst gerade den Fall verschwinden ließe, den man darin sehen will.
* **Update**: Das Fixture in `auth.middleware.spec.ts` setzte denselben falschen Feldnamen wie die Middleware und zementierte damit den Bug, statt ihn zu finden — umgestellt auf `role`. Die Rollen-Fälle liegen jetzt in `authorize.middleware.spec.ts`, die bewusst **ohne** `vi.mock` der users-Domain gegen die echte Matrix läuft; mit gemocktem `hasEffectivePermission` wäre nur gemessen, dass die Middleware etwas fragt, nicht was dabei herauskommt.
* **Hinweis**: Offen als Folge-Arbeit: `apikeyDataSchema` erlaubt `role` aus allen `UserSystemRole`-Werten, `PLATFORM_OWNER` eingeschlossen, für den `printServerAuthorize` einen Bypass hat. Das ist keine neue Lücke — `channels.ts` liest die Rolle seit jeher korrekt, ein solcher Schlüssel hat über die Feathers-Services längst weitreichende Rechte. Die Allowlist gehört an die Ausstellung, in **beiden** Repos (panary-cloud hat denselben Resolver unter `apps/api-cloud/src/services/apikeys/apikeys.schema.ts`).
