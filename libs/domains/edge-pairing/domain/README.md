# edge-pairing-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build edge-pairing-domain` to build the library.

## Running unit tests

Run `nx test edge-pairing-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
