# Commits, die `git blame` überspringen soll: rein mechanische Umformatierungen
# ohne Verhaltensänderung. GitHub wertet diese Datei automatisch aus. Lokal
# einmalig scharf schalten:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Aufnahmekriterium: Der Commit ändert ausschließlich Formatierung, steht allein
# (keine Sachänderung daneben) und ist als solcher in der Message gekennzeichnet.

# chore(format): Prettier über den gesamten Bestand ziehen — rein mechanisch (341 Dateien)
29b2ba668d140afc962767b17962effc522930f3
