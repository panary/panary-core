* **Update**: Regeldateien unter `.claude/rules/` laden nur noch bei passenden Pfaden (`paths:`-Frontmatter, #318). §10 Spec-Isolation, Test-Timeouts und Runner-Konfiguration ist aus `code-style.md` nach `testing.md` ausgelagert (`*.spec.ts`, vitest-Configs, `project.json`); `angular.md` lädt bei POS-, Setup- und Admin-Client sowie `feature-*`, `ui-*` und `data-access*`-Libs, `logging.md` bei `apps/api-edge/**` und `libs/shared/backend/**`, `documentation.md` bei `docs/**`. `security.md`, `data-models.md` und der Rest von `code-style.md` laden weiter immer. Abschnittsnummer §10 unverändert. Immer geladen: 33 KB statt 67 KB, gemessen mit `cat CLAUDE.md $(grep -L '^paths:' .claude/rules/*.md) | wc -c`. Gesamtzusammenhang: panary/panary-workbench#31, Schwesterschritt panary/panary-cloud#431.
