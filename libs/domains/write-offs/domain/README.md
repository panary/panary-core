# write-offs-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build write-offs-domain` to build the library.

## Running unit tests

Run `nx test write-offs-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
