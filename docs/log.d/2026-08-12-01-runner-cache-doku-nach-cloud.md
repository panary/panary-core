* **Deprecation**: Self-hosted-Runner- und Nx-Remote-Cache-Doku nach `panary-cloud/docs/infrastructure/` (privat) verschoben — sie beschreiben CI-Infra, die ausschließlich das private panary-cloud bedient, und enthielten interne Adressen (IPs/Hostnamen), die nicht in ein öffentliches Repo gehören. In core entfernt; der Cache-Wrapper `tools/nx-cache/Dockerfile` (unkritisch, nur Env-Var-Namen) bleibt hier.
