# devices-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build devices-domain` to build the library.

## Running unit tests

Run `nx test devices-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
