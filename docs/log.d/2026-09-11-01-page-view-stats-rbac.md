* **RBAC**: `AppResource.STOREFRONT_PAGE_VIEW_STATS` (`storefront-page-view-stats`, cloud-only, find-only) mit READ für TENANT_OWNER/MANAGER/STAFF und PLATFORM_ADMIN/SUPPORT in der Matrix — Lese-Seite des Storefront-Aufrufe-Zählers (panary-cloud#330, dort ADR 0060). Kein eigenes Release: der Eintrag fährt beim nächsten `v*`-Tag mit; bis cloud den Stand pinnt, läuft der Pfad dort über `PUBLIC_AUTHENTICATED_PATHS`, eine Spec in cloud erzwingt danach das Aufräumen.
