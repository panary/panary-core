# order-interactions-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build order-interactions-domain` to build the library.

## Running unit tests

Run `nx test order-interactions-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
