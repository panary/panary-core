{
  "name": "notifications",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domains/notifications",
  "projectType": "library",
  "tags": ["type:domain-package", "domain:notifications", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/libs/domains/notifications/domain/dist"],
      "dependsOn": ["notifications-domain:build"],
      "options": {
        "command": "echo 'notifications package build complete'"
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "libs/domains/notifications"
      }
    }
  }
}
