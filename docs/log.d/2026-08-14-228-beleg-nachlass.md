* **Update**: Der Beleg weist den gewährten Nachlass aus (panary/panary-core#228). Neues Snapshot-Feld `discounts` (Name + abgezogener Brutto-Betrag aus `appliedDiscounts.computedAmountCents`), gefüllt von `buildReceiptSnapshot`, gedruckt vom ESC/POS-Renderer und im Abrufpfad `buildReceiptHtml`. Damit rechnet sich der Beleg auf: Positionen 11,90 − Nachlass 2,38 = Gesamt 9,52 (Zahlen der Test-Order aus [rabatte.md](../domains/rabatte.md)).
* **Update**: [rabatte.md](../domains/rabatte.md) — Abschnitt „Nachlass auf dem Beleg" mit den drei Entscheidungen, die im Diff nur als Kommentar stehen: Bestand wird **nicht** nachgerechnet (KassenSichV; Migration ohne Default/Backfill, `NULL` = kein Rabatt oder vor #228), rabattfreie Belege lassen das Feld ganz weg (kanonische JSON byte-identisch → gleicher `renderHash` wie vorher), 0-ct-Rabatte und leere Rabattnamen werden abgefangen.
* **Befund**: 🚨 **Der POS-Bon hat dieselbe Lücke — unverändert.** `order-receipt.renderer.ts` druckt unrabattierte Positionspreise und ein über `computeOrderTax` rabattiertes „Gesamt". Das ist das Papier, das der Gast an der Kasse bekommt; #228 hat nur den elektronischen Beleg geheilt (anderes Artefakt, nicht in den betroffenen Pfaden des Issues).
* **Befund**: ⚠️ `receipt.lineItems[].lineTotal` ist `amount × price` und ignoriert Modifier, Menü-Komponenten und `FIXED_PROPORTIONAL` — bei solchen Zeilen rechnet sich der Beleg auch mit Nachlasszeile nicht auf. Kanonisch wäre `lineItemGrossCents` (`@panary/orders/domain`), die der POS-Bon bereits nutzt.
* **Update**: Mutationsprobe zu den neuen Specs gefahren, damit „grün" etwas bedeutet: `toReceiptDiscounts` auf leer gesetzt → 6 Builder-Tests rot, die Nachlass-Schleife im ESC/POS-Renderer entfernt → 4 Renderer-Tests rot. Die Negativfälle (kein Rabatt, `discounts: null` aus SQLite) blieben in beiden Läufen grün, wie sie sollen.
