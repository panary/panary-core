# orders-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orders-domain` to build the library.

## Running unit tests

Run `nx test orders-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
