* **Update**: [Lockfile-Auflösung des Security-Scans](../security/security-scan-lockfile-aufloesung.md): `scripts/security-scan.mjs` ist wieder **byte-identisch** mit panary-cloud. Dort wurde der Stand aus [#354](https://github.com/panary/panary-core/issues/354) aufgegriffen und um einen eigenen Befund erweitert ([panary/panary-cloud#490](https://github.com/panary/panary-cloud/issues/490)): cloud hat **zwei** committete Lockfiles, von denen der lokale Scan nur eines maß. Die Auflösung läuft jetzt in beiden Repos über `git ls-files '*pnpm-lock.yaml'` — eine echte Obermenge, die hier genau ein Lockfile liefert.
* **Update**: 🚫 **Wörtlich kopieren wäre falsch gewesen.** Die cloud-Fassung trug bare Issue-Nummern in den Kommentaren; `#274` ist in diesem Repo „Kassenbon druckt Uhrzeit in UTC statt Filialzeit", ein völlig anderes Ticket, und `#490` gibt es hier noch nicht — es wird es aber irgendwann geben. Dazu Sätze wie „this repo has TWO committed ones", die hier schlicht falsch sind. Der Kommentarteil ist deshalb **repo-neutral** formuliert: Wo repo-spezifische Tatsachen nötig sind, werden beide Repos benannt (in beiden wahr), Issue-Verweise sind vollqualifiziert.
* **Update**: **Kein Verhaltens-Delta — gemessen, nicht behauptet.** Gerade weil keines erwartet war, wurden beide Lagen gegen den alten Stand geprüft (`origin/main` @ `9a4baadd`, osv-scanner 2.3.8): Arbeitsbaum → `Arbeitsbaum`, 0 Befunde in beiden Fassungen; **lebender** Symlink aus dem Repo heraus → `committeter Stand HEAD@9a4baadd`, 0 Befunde in beiden. Der Symlink-Schutz aus #354 greift unverändert. Einziger Unterschied ist die Wortwahl der Lockfile-Zeile (`lockfile:` → `1 Lockfile:`); Befunde tragen ihre Quelldatei erst ab dem zweiten Lockfile, hier also nie.
* **Update**: Mutationsprobe als Gegenprobe zum grünen Lauf: `adm-zip@0.6.0` eingeschleust → **2 Befunde** (GHSA-7q85-xj36-vmfc high, GHSA-vwc7-r8mq-g2x9 medium). Ohne sie wäre „0 Befunde" nicht von „nicht hingesehen" zu unterscheiden. ⚠️ Das Advisory darf **nicht** auf der Ignore-Liste stehen — mit `image-size` gemessen wäre die Probe grün und wertlos gewesen.
* **Update**: ⚠️ **Die Symlink-Probe zeigte bewusst nicht aufs Workbench-Root-Lockfile.** Ein `git checkout -- pnpm-lock.yaml` schreibt durch einen solchen Symlink hindurch und überschreibt die **ungetrackte** Datei am Workbench-Root — per git nicht wiederherstellbar. Das Probenziel lag in einem Wegwerf-Verzeichnis, zurückgesetzt wurde mit `rm` **vor** `git checkout`, Hash vorher/nachher verglichen.
* **Update**: Der Punkt „panary-cloud wurde nicht abgeglichen" unter „Was das nicht löst" ist geschlossen. Neu dafür: **Deckungsgleichheit ist ein Zustand, kein Gate** — nichts hindert den nächsten einseitigen Edit, und keine der beiden CIs prüft die Dateien gegeneinander. Der Einzeiler zum Nachmessen steht in der Doku.
