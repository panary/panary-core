{
  "name": "devices",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domains/devices",
  "projectType": "library",
  "tags": [
    "type:domain-package",
    "domain:devices",
    "publishable"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/libs/domains/devices/domain/dist"
      ],
      "dependsOn": [
        "devices-domain:build"
      ],
      "options": {
        "command": "echo 'devices package build complete'"
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "libs/domains/devices"
      }
    }
  }
}
