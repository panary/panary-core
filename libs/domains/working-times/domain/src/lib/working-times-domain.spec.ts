import { workingTimesDomain } from './working-times-domain'

describe('workingTimesDomain', () => {
  it('should work', () => {
    expect(workingTimesDomain()).toEqual('working-times-domain')
  })
})
