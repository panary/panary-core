* **Update**: Der Bootstrap-Push Edge→Cloud verträgt jetzt `null` aus leeren SQLite-Spalten. `locations.brandId/handle/locale/defaultCurrency` und `users.staffRole/posPin/discountDetails` waren `Type.Optional(Type.String())` — das erlaubt nur `undefined`, während der Edge die Rows roh aus SQLite liest. Der Push wies die Location mit `/brandId: must be string` ab und riss den gesamten Bootstrap mit, weil `locations` der erste Service in `MASTER_DATA_SERVICES` ist und der Runner beim ersten Fehler wirft. Regel und Hintergrund im [Cloud-Pairing-Wizard](../architecture/cloud-pairing-wizard.md).
* **Update**: Der destruktive Modus `pull-cloud-to-edge` leert den Edge jetzt tatsächlich. Der Bulk-`remove(null, { query })` funktioniert nur mit `remove` in den `multi`-Adapter-Optionen — das hat allein `products`; für die übrigen sieben Master-Services quittierte der Adapter mit „Can not remove multiple entries", der Runner degradierte still auf `upsert` und meldete trotzdem „Bootstrap erfolgreich abgeschlossen", obwohl `confirmDataLoss` bestätigt war. Neu in [`truncate-master-tables.ts`](../../apps/api-edge/src/workers/truncate-master-tables.ts): Bulk versuchen, sonst einzeln löschen, Ergebnis verifizieren — und bei Rest abbrechen statt einen gemischten Bestand als Erfolg zu führen.
* **Update**: Offene Sync-Konflikte lassen sich wieder auflösen. `syncConflictPatchSchema` kannte nur `resolution`, ist aber geschlossen — `multiTenancy()` stempelt `tenantId` in `around.all` und damit vor `validateData`, weshalb jeder Klick auf „Diesen Standort behalten" bzw. „Verwerfen" auf 400 lief. Dritte Wiederholung derselben Klasse, diesmal in einem handgeschriebenen Patch-Schema; der Bestandsabschnitt in [gestempelte Felder gehören ins Schema](../security/gestempelte-felder-in-schemas.md) ist entsprechend korrigiert.
