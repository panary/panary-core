# customers-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build customers-domain` to build the library.

## Running unit tests

Run `nx test customers-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
