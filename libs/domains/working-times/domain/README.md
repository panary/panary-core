# working-times-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build working-times-domain` to build the library.

## Running unit tests

Run `nx test working-times-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
