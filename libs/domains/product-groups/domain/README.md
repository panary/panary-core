# product-groups-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build product-groups-domain` to build the library.

## Running unit tests

Run `nx test product-groups-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
