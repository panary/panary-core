# cloud-edges-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cloud-edges-domain` to build the library.

## Running unit tests

Run `nx test cloud-edges-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
