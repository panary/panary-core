{
  "name": "order-references",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domains/order-references",
  "projectType": "library",
  "tags": ["type:domain-package", "domain:order-references", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/libs/domains/order-references/domain/dist"],
      "dependsOn": ["order-references-domain:build"],
      "options": {
        "command": "echo 'order-references package build complete'"
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "libs/domains/order-references"
      }
    }
  }
}
