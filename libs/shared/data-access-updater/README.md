# data-access-updater

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-access-updater` to build the library.

## Running unit tests

Run `nx test data-access-updater` to execute the unit tests via [Vitest](https://vitest.dev/).
