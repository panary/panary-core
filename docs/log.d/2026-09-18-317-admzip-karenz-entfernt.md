* **Update**: [OSV-Befund 2026-09-10](../security/osv-befund-2026-09-10.md) um den Nachtrag zum Entfernen der `adm-zip`-Karenzausnahme erweitert. `minimumReleaseAgeExclude` in `pnpm-workspace.yaml` trug `adm-zip`, weil 0.6.1 erst am 2026-09-18 10:24 UTC karenzreif wurde; ab diesem Zeitpunkt ist der Eintrag ein stiller Karenz-Verzicht für genau das Paket, das gerade gepatcht wurde. Der Override-Floor `^0.6.1` in `package.json` bleibt — er ist die Absicherung, der Exclude war nur die Brücke. Nachweis über die **Lockfile-Version** (`adm-zip@0.6.1`, unverändert), nicht über den Exit-Code von `pnpm install`.
