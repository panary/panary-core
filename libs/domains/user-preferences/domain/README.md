# user-preferences-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build user-preferences-domain` to build the library.

## Running unit tests

Run `nx test user-preferences-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
