* **Update**: [Tagesabschluss-Architektur](../domains/tagesabschluss-architektur.md): Verweis auf die drei LikeC4-Ablauf-Views, die panary/panary-cloud#187 angelegt hat — `dynBusinessDayClosing` (cloud-verwalteter Regelfall), `dynBusinessDayClosingEdge` (der hier beschriebene Edge-Pfad, nur ohne aktives Pairing erreichbar) und `dynBusinessDayRotation`. Die Nummer im Dateinamen ist die Issue-Nummer im **Nachbar-Repo**; das Modell liegt dort (ADR 0028), der Ablauf beginnt aber hier.
