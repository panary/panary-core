{
  "name": "auth",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domains/auth",
  "projectType": "library",
  "tags": [
    "type:domain-package",
    "domain:auth",
    "publishable"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/libs/domains/auth/domain/dist"
      ],
      "dependsOn": [
        "auth-domain:build"
      ],
      "options": {
        "command": "echo 'auth package build complete'"
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "libs/domains/auth"
      }
    }
  }
}
