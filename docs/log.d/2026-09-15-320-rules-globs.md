* **Fix**: Die `paths:`-Globs aus #318 erfassten zwei Bereiche nicht (#320). `angular.md` lädt jetzt auch für `libs/apps/**` — unter `libs/apps/pos-client/shell/` liegen echte Standalone-Komponenten (`shell.ts`, `idle-warning-overlay.component.ts`), die kein bestehender Glob traf. `testing.md` lädt jetzt auch für Projekte, deren Vitest-Runner `vite.config.*` heißt; davon gibt es 32, alle mit `test:`-Block, gegenüber 81 `vitest.config.*`.
