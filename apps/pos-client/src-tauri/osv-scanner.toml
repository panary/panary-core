# OSV-Scanner: bewusst akzeptierte Advisories für die Tauri-Linux-Build-Deps
# (Cargo.lock in diesem Verzeichnis). osv-scanner v2.3.8 meldet strenger als
# v2.0.2; die folgenden Findings sind unmaintained-Crates ohne Fix bzw. an den
# Tauri-GTK3-Stack gebundene Transitiv-Deps. Regelmäßig re-evaluieren.
# Scope: nur diese Datei (Auto-Discovery neben Cargo.lock) — pnpm-Scan bleibt streng.

[[IgnoredVulns]]
id = "RUSTSEC-2024-0413"  # atk
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0416"  # atk-sys
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0412"  # gdk
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0418"  # gdk-sys
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0411"  # gdkwayland-sys
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0417"  # gdkx11
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0414"  # gdkx11-sys
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0415"  # gtk
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0420"  # gtk-sys
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0419"  # gtk3-macros
reason = "gtk-rs GTK3-Binding unmaintained (kein Fix verfügbar); transitiv über Tauri v2 (webkit2gtk/GTK3). Migration erst mit Tauri-GTK4-Stack."

# glib: osv-scanner meldet den Befund unter der GHSA-ID und filtert den
# RUSTSEC-Alias automatisch mit ("… and 1 alias have been filtered out").
# Ein zusaetzlicher RUSTSEC-2024-0429-Eintrag bliebe ungenutzt und wuerde als
# "has unused ignores" gemeldet — deshalb nur die GHSA-ID.
[[IgnoredVulns]]
id = "GHSA-wrw7-89jp-8q8g"  # glib (Alias: RUSTSEC-2024-0429)
reason = "gtk-rs glib 0.18 (CVSS 6.9 Medium). Fix erst mit glib 0.20 — an den gtk-rs/Tauri-GTK3-Stack gebunden, nicht isoliert bumpbar. Bewusst akzeptiert bis Tauri-Stack-Upgrade."

[[IgnoredVulns]]
id = "RUSTSEC-2024-0370"  # proc-macro-error
reason = "unmaintained Proc-Macro-Helper (kein Fix); transitive Build-Abhängigkeit."

[[IgnoredVulns]]
id = "RUSTSEC-2025-0081"  # unic-char-property
reason = "unmaintained Unicode-Crate (kein Fix); transitive Build-Abhängigkeit im Tauri-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2025-0075"  # unic-char-range
reason = "unmaintained Unicode-Crate (kein Fix); transitive Build-Abhängigkeit im Tauri-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2025-0080"  # unic-common
reason = "unmaintained Unicode-Crate (kein Fix); transitive Build-Abhängigkeit im Tauri-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2025-0100"  # unic-ucd-ident
reason = "unmaintained Unicode-Crate (kein Fix); transitive Build-Abhängigkeit im Tauri-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2025-0098"  # unic-ucd-version
reason = "unmaintained Unicode-Crate (kein Fix); transitive Build-Abhängigkeit im Tauri-Stack."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0194"  # quick-xml (DoS: quadratische Attribut-Duplikat-Prüfung)
reason = "quick-xml 0.39.4 — Fix erst in 0.41.0, kein 0.39.x-Backport. Einziger Parent plist 1.9.0 (neueste Version, via tauri 2) verlangt strikt ^0.39.2 → nicht isoliert bumpbar. Exposure: plist parst nur eigene Bundle-Metadaten (Info.plist) zur Build-/Bundle-Zeit, kein Angreifer-kontrolliertes XML zur POS-Laufzeit. Re-evaluieren sobald plist quick-xml ≥0.41 erlaubt."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0195"  # quick-xml (DoS: unbegrenzte Namespace-Allokation in NsReader)
reason = "quick-xml 0.39.4 — Fix erst in 0.41.0, kein 0.39.x-Backport. Einziger Parent plist 1.9.0 (neueste Version, via tauri 2) verlangt strikt ^0.39.2 → nicht isoliert bumpbar. Exposure: plist parst nur eigene Bundle-Metadaten (Info.plist) zur Build-/Bundle-Zeit, kein Angreifer-kontrolliertes XML zur POS-Laufzeit. Re-evaluieren sobald plist quick-xml ≥0.41 erlaubt."

# rkyv ist der einzige Eintrag hier, der NICHT kompiliert wird — anders als alle
# obigen (die sind im Binary, nur nicht fixbar). Der Scanner liest Cargo.lock,
# und Cargo.lock fuehrt optionale Deps von Deps unabhaengig davon, ob das Feature
# aktiv ist. Belegt mit:
#   cargo tree --locked --target all -e normal | grep -c rkyv   → 0
#   cargo tree --locked --target all -i rkyv                    → "nothing to print"
# Aktiv an rust_decimal ist nur `std` (via byte-unit), nicht `rkyv`.
[[IgnoredVulns]]
id = "RUSTSEC-2026-0235"  # rkyv (OOB-Read bei Rc/Arc-Archiven mit gefaelschter Slice-Laenge)
reason = "rkyv 0.7.46 wird NICHT kompiliert: optionale Dependency von rust_decimal, deren `rkyv`-Feature nirgends aktiviert ist (nur `std`) — steht ausschliesslich im Lockfile. Nicht bumpbar: rust_decimal 1.42.1 (bereits hoechste mit byte-unit ^1.15 vereinbare Version) verlangt strikt ^0.7.46, und der Fix existiert nur in 0.8.17 — die gesamte 0.7-Linie ist betroffen, ohne Backport. Kette: panary-pos → tauri-plugin-log ^2 → byte-unit ^5 → rust_decimal ^1.15. Exposure: das Advisory braucht `rkyv::access`/`rkyv::from_bytes` auf Angreifer-kontrollierten Archiven; der POS deserialisiert nirgends rkyv-Archive, und der Code ist ohnehin nicht im Binary. Re-evaluieren, sobald byte-unit/rust_decimal auf rkyv 0.8 gehen — dann faellt der Eintrag ersatzlos weg."
