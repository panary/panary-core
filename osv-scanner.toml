# OSV-Scanner: bewusst akzeptierte Advisories fuer das npm-Lockfile in DIESEM
# Verzeichnis (pnpm-lock.yaml). osv-scanner sucht seine Konfiguration nur im
# Verzeichnis des gescannten Manifests und vererbt sie NICHT an
# Unterverzeichnisse — die Cargo-Ausnahmen des Tauri-Stacks stehen deshalb
# weiterhin getrennt in apps/pos-client/src-tauri/osv-scanner.toml.
#
# Verlauf: docs/security/osv-befund-2026-08-07.md (image-size eingetragen),
# docs/security/osv-befund-2026-09-26.md (image-size entfernt).
# Pendant in panary-cloud/osv-scanner.toml — synchron halten.

# --- Derzeit keine akzeptierten Befunde --------------------------------------
# Die Datei bleibt trotzdem stehen: scripts/security-scan.mjs reicht sie per
# --config an osv-scanner weiter, damit ein kuenftiger Eintrag fuer jedes
# gescannte Lockfile gilt — auch fuer die Temp-Kopie aus HEAD, die im
# Haupt-Checkout gescannt wird.
#
# Zuletzt entfernt am 2026-09-26: GHSA-5p2g-fcmc-qvqq (JXL/HEIF) und
# GHSA-w3rx-r6r6-pgpr (ICNS) zu image-size. Seit dem 2026-09-24 erfassen beide
# image-size@0.5.5 nicht mehr (ICNS erst ab 0.6.3, JXL/HEIF ab 1.2.0, Fix
# 2.0.3), und osv-scanner meldete die Eintraege als "unused ignores" — bei
# Exit 0. Ein Override-Floor ^2.0.4 waere kein Ersatz gewesen: Er haette less
# (~0.5.0) auf eine API-inkompatible Major-Version gezwungen.
