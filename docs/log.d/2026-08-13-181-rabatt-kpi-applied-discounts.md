* **Fix**: Die Rabatt-KPI des Tagesabschlusses zählt jetzt `appliedDiscounts`. `aggregateFinancials` las bis dahin **ausschließlich** `order.discount` — und weil der discount-mutex genau dieses Feld leert, sobald `appliedDiscounts` gesetzt sind, zählte jeder über den POS gewährte Rabatt als **0 Rabatte / 0,00 €**: im Z-Bon, in der Rabatt-Quote der Cloud-Auswertung und in der Karte „Finanzen" des Tagesabschlusses. Summiert wird `computedAmountCents`, der von `computeOrderTax` zurückgeschriebene tatsächlich abgezogene Brutto-Betrag — keine Rückrechnung nötig. `discountsCount` bleibt bewusst **pro Order** statt pro Rabatt-Eintrag: Der Wert speist die Quote rabattierter Bestellungen, und eine Order mit zwei Rabatten ist eine rabattierte Order. `order.discount` bleibt als Fallback für Bestands-Orders, bis das Feld selbst entfällt (Schritt 3 von panary/panary-core#181, [ADR 0030](../adr/0030-legacy-rabattfeld-abgeschafft.md)).
* **Update**: `makeOrder` (Aggregator-Fixtures) nimmt `discount` und `appliedDiscounts` entgegen, dazu ein `makeAppliedDiscount`-Builder. Die Rabatt-KPI war vorher in `financials.spec.ts` **gar nicht** abgedeckt — weder der neue noch der Legacy-Pfad.
