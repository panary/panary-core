# order-references-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build order-references-domain` to build the library.

## Running unit tests

Run `nx test order-references-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
