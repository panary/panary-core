import { openingHourExceptionsDomain } from './opening-hour-exceptions-domain'

describe('openingHourExceptionsDomain', () => {
  it('should work', () => {
    expect(openingHourExceptionsDomain()).toEqual('opening-hour-exceptions-domain')
  })
})
