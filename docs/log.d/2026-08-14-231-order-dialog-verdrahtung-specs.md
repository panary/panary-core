* **Update**: [Rabatte](../domains/rabatte.md) — Abschnitt „Testnetz der POS-Verdrahtung": 25 Charakterisierungs-Specs decken Reset-Pfade, Gates und den `placeOrder`-Snapshot-Bau des Order-Dialogs ab (bis dahin ohne Netz, obwohl die ausgelagerte Logik durchgetestet ist). Zwei Bestandsbefunde festgehalten statt behoben — der verschluckte Rabattcode-Hinweis und der still verworfene Order-Rabatt beim Wechsel auf Personalessen. Die frühere Aussage „läuft ohne Code weiter (mit Hinweis)" ist entsprechend korrigiert: Der Hinweis wird von `unselectProduct()` überschrieben, bevor ihn jemand liest.
* **Update**: [Test-Target an einer Nx-Lib nachrüsten](../guides/lib-vitest-test-target.md) — vierte Falle ergänzt: Ein `effect()` im Konstruktor lässt `Injector.create()` mit `NG0201 (ChangeDetectionScheduler)` scheitern, danach mit `node.scheduler.add is not a function`. Die drei nötigen Provider (`ɵChangeDetectionScheduler`, `ɵEffectScheduler`, `DestroyRef`) stehen jetzt dort.
* **Update**: `orders-feature-pos-order-dialog` hat eine `tsconfig.spec.json` bekommen — die Lib hatte ein `typecheck`-Target, aber keine spec-tsconfig; ihre sechs Spec-Dateien wurden deshalb nie typgeprüft (Guide §2). Typecheck ist danach grün, es blieb bei der Lücke ohne Folgeschaden.
